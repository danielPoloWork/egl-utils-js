---
---

Release infrastructure only (roadmap 7.4): changesets, the provenance publish
workflow, supply-chain gates, and the version-lockstep tool. No change to the
library's public API or behaviour, so no version bump — recorded as an empty
changeset rather than left absent, so `changeset status` stays meaningful.
