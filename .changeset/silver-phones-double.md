---
---

Documentation pass only (roadmap 7.5): generated API reference (TypeDoc), README usage
examples for the full public surface, and a SECURITY.md section on sanitizeHtml's
non-goals. No behavior change, so no version bump — recorded as an empty changeset rather
than left absent.
