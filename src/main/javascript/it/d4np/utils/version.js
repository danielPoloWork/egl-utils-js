/**
 * Package version constant. Kept in lockstep with `package.json` "version" and
 * the README `Status` badge (`tools/consistency_lint.py`'s version-lockstep
 * check); bump all three together on release, never independently.
 *
 * @module egl-utils-js/version
 */

export const VERSION = '0.0.0';
